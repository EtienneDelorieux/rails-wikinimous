<h1>All my hipster articles</h1>

<div class="articles">
  <% @articles.each do |article| %>
    <article class="articles__item">
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content %></p>
    </article>
  <% end %>
</div>
